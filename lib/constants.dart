///These keys are revoked. Use keys and product ids from Stripe dashboard.
const apiKey = 'YOUR PUBLIC KEY';
const secretKey = 'YOUR SECRET KEY';

//Goto stripe dashboard add a product put its price and then copy its id and then paste it
const nikesPriceId = 'price_1M4sRFLoj1LIEBIlt66cSZ5m';
